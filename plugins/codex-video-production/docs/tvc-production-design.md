# TVC Production Design

当前已冻结的 TVC 实现设计是 [File-tree Production Design](file-tree-production-design.md)。该文档回答目录结构、Git/Decision 绑定、Task 账本、Stage checkpoints、Preview Stage Views、并发原子性和代码删除预算，是唯一实现基线。

旧的单文件 Production、版本分层、领域 revision 数组与 `product-ad` 兼容设计已被替换，不再是实现或恢复依据。
